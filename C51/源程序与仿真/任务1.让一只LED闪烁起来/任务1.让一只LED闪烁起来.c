/**任务1.让一只LED闪烁起来**/
//===============声明区===============================
#include <reg52.h>				//包含库函数reg52.h进来
sbit led=P0^0;					//声明变量led指向单片机的P0.0口
//===============主程序===============================
main()				   			//主函数，void指空类型，没有返回值
{
 	unsigned int i;				//声明i是一个无符号整型变量（0~65535）
	while(1)			   		//无穷循环
	{
	 	led=1;			  		//P0.0设置为高电平
		for(i=0;i<30000;i++);	//循环30000次，延时一段时间
	 	led=0;			  		//P0.0设置为低电平
		for(i=0;i<30000;i++);	//循环30000次，延时一段时间
	}
}